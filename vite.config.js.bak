import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [
    react({
      jsxRuntime: 'automatic' // ✅ IMPORTANTE
    }),
    tailwindcss()
  ],
  resolve: {
    alias: {
      "@": "/src",
    },
  },
  build: {
    outDir: "dist",
  },
server: {
  host: '127.0.0.1', // 👈 força Vite a rodar com IP local
  port: 5173,
  proxy: {
    '/api': {
      target: 'http://127.0.0.1:5000', // 👈 também mantém o mesmo domínio
      changeOrigin: true,
      secure: false,
   
    },
  },
},

})